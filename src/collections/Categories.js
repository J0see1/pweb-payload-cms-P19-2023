/** @type {import('payload/types').CollectionConfig} */
const Categories = {
  slug : "Categories",
  fields : [
    {
      name: "Category",
      type: "text",
      required: true,
    },
  ]
}

export default Categories